//异或：相同为0，不同为1. 异或同一个数两次，原数不变。
class Solution {
    public int singleNumber(int[] nums) {
       int result=0;
       for(int i=0;i<nums.length;i++){
           result^=nums[i];
       }
        return result;
    }
}
